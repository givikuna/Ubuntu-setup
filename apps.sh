# Google Chrome
flatpak install --assumeyes flathub com.google.Chrome

# Boxes
flatpak install --assumeyes flathub org.gnome.Boxes

# VSCodium
flatpak install --assumeyes flathub com.vscodium.codium

# Beekeeper Studio
sudo snap install beekeeper-studio  --non-interactive

# Sublime Text
sudo snap install sublime-text --classic --non-interactive

# VSCode
sudo snap install code --classic --non-interactive

# Notepad++
sudo snap install notepad-plus-plus --non-interactive

# GNU Emacs
sudo snap install emacs --classic --non-interactive

# Eclipse
sudo snap install eclipse --classic --non-interactive

# Android Studio
sudo snap install android-studio --classic --non-interactive

# GitKraken
sudo snap install gitkraken --classic --non-interactive

# Jenkins
sudo snap install jenkins --classic --non-interactive

# Arduino IDE
sudo snap install arduino --non-interactive

# Sublime Merge
sudo snap install sublime-merge --classic --non-interactive

# Aliasman
sudo snap install aliasman --non-interactive

# Meld
flatpak install --assumeyes flathub org.gnome.meld

# htop
sudo apt-get install htop

# Audacity
flatpak install --assumeyes flathub org.audacityteam.Audacity

# Kdenlive
flatpak install --assumeyes flathub org.kde.kdenlive

# RetroArch
flatpak install --assumeyes flathub org.kde.kdenlive

# Telegram Desktop
flatpak install --assumeyes flathub org.telegram.desktop

# hardware Probe
flatpak install --assumeyes flathub org.linux_hardware.hw-probe

# Atom
flatpak install --assumeyes flathub io.atom.Atom

# Eyedropper
flatpak install --assumeyes flathub com.github.finefindus.eyedropper

# Discord
sudo snap install discord --non-interactive

# Signal
sudo snap install signal-desktop --non-interactive

# Steam
sudo snap install steam --non-interactive

# Spotify
sudo snap install spotify --non-interactive

# OBS
flatpak install --assumeyes flathub com.obsproject.Studio
